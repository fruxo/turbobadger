language: cpp
compiler:
  - gcc
  - clang

sudo: false
dist: bionic

addons:
  apt:
    packages:
    - cmake
    - clang
    - libxl1-dev
    - libgl1-mesa-dev
    - libxrandr-dev
    - libwayland-dev

before_install:
#  - sudo apt-get update -qq;
  - ${CXX} --version;

script: 
  - echo ${CXX}
  - make sdl2
  - make glfw

