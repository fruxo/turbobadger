language: cpp
compiler:
  - gcc
  - clang

sudo: false
dist: bionic

addons:
  apt:
    packages:
    - cmake
    - clang
    - libsdl2-2.0-0

before_install:
#  - sudo apt-get update -qq;
  - ${CXX} --version;

script: 
  - echo ${CXX}
  - make sdl2
  - make glfw

